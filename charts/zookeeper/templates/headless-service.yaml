apiVersion: v1
kind: Service
metadata:
  name: {{ include "zookeeper.fullname" . }}-headless
spec:
  clusterIP: None
  selector:
    app: {{ include "zookeeper.name" . }}
  ports:
    - name: client
      port: 2181
    - name: peer
      port: 2888
    - name: leader
      port: 3888

