apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "solr-kafka-platform.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "solr-kafka-platform.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "solr-kafka-platform.name" . }}
    spec:
      containers:
        - name: solr
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8983

