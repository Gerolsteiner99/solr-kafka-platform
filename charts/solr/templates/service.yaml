apiVersion: v1
kind: Service
metadata:
  name: {{ include "solr.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "solr.name" . }}
  ports:
    - name: http
      port: 8983
      targetPort: http

